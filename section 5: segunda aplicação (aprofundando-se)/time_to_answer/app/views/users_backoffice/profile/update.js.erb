$('#avatarModal').modal('hide');

document.getElementById('imgAvatarL').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('imgAvatarR').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('imgAvatarC').src = "<%= url_for(@user.user_profile.avatar) %>"

$.bootstrapGrowl("Avatar atualizado!", {
  type: 'success', // (null, 'info', 'danger', 'success')
  align: 'right', // ('left', 'right', or 'center')
  allow_dismiss: true, // If true then will display a cross to close the popup.
  stackup_spacing: 10 // spacing between consecutively stacked growls.
});